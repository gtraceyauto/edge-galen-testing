Index: lib/gulp-galen.js
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
--- lib/gulp-galen.js	(date 1457428479000)
+++ lib/gulp-galen.js	(revision )
@@ -2,7 +2,10 @@
 
 var spawn = require("child_process").spawn,
   path = require("path"),
-  through = require('through');
+  through = require('through'),
+  npm = require('npm'),
+  fs = require('fs'),
+  which = require('which');
 
 function replacePlaceholders(obj, placeholders) {
   if (typeof obj === "string") {
@@ -31,7 +34,13 @@
       opt[key] = options[key];
     });
 
-    var galenPath = "/usr/local/bin/galen";
+    var galenPath;
+    try {
+      galenPath = opt.galenPath || which.sync('galen');
+    } catch (e) {
+      stream.emit('error', new Error("Could not find galen in $PATH."));
+    }
+
     if (opt.galenPath) {
       galenPath = opt.galenPath;
       delete opt.galenPath;
Index: test/basics.js
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
--- test/basics.js	(date 1457428479000)
+++ test/basics.js	(revision )
@@ -17,8 +17,7 @@
 
     var options = {
       url: "https://www.google.com",
-      size: "800x600",
-      galenPath: "./node_modules/galenframework/bin/galen"
+      size: "800x600"
     };
 
     it("should iterate over some gspecs", function (done) {
@@ -59,7 +58,6 @@
         .pipe(gulpGalen.check({
           url: "https://www.google.com",
           size: "800x600",
-          galenPath: "./node_modules/galenframework/bin/galen",
           testngreport: "./tmp/test-reports/testng-{basename}.xml"
         }))
         .pipe(es.writeArray(function (err, arr) {
\ No newline at end of file
Index: package.json
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
--- package.json	(date 1457428479000)
+++ package.json	(revision )
@@ -4,7 +4,8 @@
   "description": "A gulp-plugin for using the galen-framework within a gulp-based build toolchain",
   "homepage": "https://github.com/Galeria-Kaufhof/gulp-galen",
   "dependencies": {
-    "through": "^2.3.8"
+    "through": "^2.3.8",
+    "which": "^1.2.4"
   },
   "devDependencies": {
     "event-stream": "^3.3.2",
